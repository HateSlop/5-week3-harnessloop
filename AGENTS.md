# Repository Instructions

## 프로젝트 목적

이 Repository는 **Harness & Loop Engineering 기반 LLM Agent 세션**의 교육 자료와 실습 코드를 개발하기 위한 프로젝트다.

각 실습의 교육적 목적이 코드에서 명확하게 드러나도록 구현하며, 앞선 실습에서 배운 구조를 다음 실습에서 점진적으로 확장한다. 실습 1·2·3 Notebook은 각각 독립적으로 실행할 수 있어야 한다.

기본 개발 환경은 **Python 3.12**를 사용한다.

---

## Context 및 Source of Truth

Notion에는 이 프로젝트의 세션 구성, 교육적 요구사항, 실습 계획이 정리되어 있다.

설계 또는 구현을 시작하기 전에 Notion MCP를 사용하여 **현재 작업에 필요한 Context만** 다음 순서로 확인한다.

1. 전체 세션 계획
2. 현재 작업과 관련된 세션 페이지
3. 현재 작업과 관련된 실습 계획
4. 필요한 경우 프로젝트 구조 초안

현재 작업과 관계없는 Notion 페이지를 불필요하게 검색하거나 읽지 않는다.

프로젝트 구조는 초안으로 취급한다. 초안에 파일이나 모듈이 표시되어 있다는 이유만으로 미리 생성하지 않는다. 각 실습의 설계와 구현 과정에서 실제로 필요해졌을 때 추가하거나 재구성한다.

기술적 사실이나 구현 방법을 확인할 때는 일반적인 모델 지식이나 프로젝트 내부 자료에만 의존하지 않고, 관련 **1차 출처를 직접 확인한다.**

출처는 다음 순서로 우선한다.

1. 해당 기술을 개발·운영하는 조직의 공식 Documentation 및 공식 기술 자료
2. 개념을 제안한 원 논문
3. 공식 Specification 및 공식 GitHub Repository
4. 프로젝트의 Notion 자료

Notion 자료와 현재의 1차 출처가 충돌하는 경우 임의로 하나를 선택하지 않는다. 현재 작업에 영향을 주는 차이라면 사용자에게 알리고 확인한 뒤 진행한다.

---

## 작업 범위

현재 실습 또는 사용자가 명시적으로 요청한 범위 안에서만 작업한다.

이후 실습에서 다룰 개념이나 기능을 미리 구현하지 않는다.

현재 교육 목표를 명확하게 보여줄 수 있는 **가장 단순한 구현**을 우선한다.

교육 목적에 필요하지 않은 과도한 abstraction, architecture, dependency, production-oriented complexity를 추가하지 않는다.

이후 실습이 이전 실습을 확장하는 경우 개념과 동작을 이어받되, 이전 Notebook을 import하지 않는다. 독립 실행에 필요한 기반 코드만 현재 Notebook에 포함하고, 공통 Tool 함수는 `tools/`에서 재사용한다.

현재 범위를 넘어서는 설계 변경이 필요하다고 판단되면 임의로 확장하지 말고 사용자에게 알린다.

---

## 실습 자료 구성

실습 1·2·3의 실행 로직과 참가자용 설명은 각 Notebook에 함께 둔다.

- `tools/`: 실습에서 공통으로 사용하는 Tool 함수
- Notebook: 해당 실습의 실행 로직, 설명, 단계별 실행과 결과 관찰을 모두 담는 참가자용 자료
- Notion: 개념 설명, 세션 구성, 실습 요구사항

각 Notebook은 새 커널에서 처음부터 끝까지 실행할 수 있어야 한다. 다른 실습 Notebook이나 `tools/` 밖의 실행용 `.py` 파일에 의존하지 않는다. `workspace/` 문서, 양식 파일과 환경 설정은 사용할 수 있다.

실습이 시작된 이후 참가자가 Notion과 번갈아 보지 않아도 되도록 구성한다.

실습에 필요한 개념 설명, 실행 단계, 관찰 포인트는 Notebook 자체에서도 이해할 수 있어야 한다.

Notebook 설명은 Notion의 [실습 Notebook 설명 작성 가이드](https://app.notion.com/p/3db05fbaaaa381c29dd9c2faf9114e6b)를 따른다. `4_hateslop/`의 Notebook은 설명 형식만 참고하고 예제 내용은 가져오지 않는다.

실습별 터미널 실행 `.py` 파일은 Notebook 완성 후 별도 단계에서 1:1로 작성한다. 현재 실습 구현의 선행 조건으로 만들지 않는다.

현재 실습 계획에서 명시적으로 요구하지 않는 한 TODO나 참가자 구현 영역을 임의로 추가하지 않는다.

---

## Notion MCP 사용 정책

Notion은 기본적으로 **reference**로 사용한다.

Read 작업은 현재 작업에 필요한 Context 범위 안에서 수행한다.

Notion 변경이 필요한 경우 다음 순서를 따른다.

1. 필요한 read-only 조사를 먼저 수행한다.
2. 구체적인 변경안을 준비한다.
3. 실제로 무엇을 변경할 것인지 사용자에게 제시한다.
4. 설정된 approval flow를 통해 승인된 경우에만 Write 작업을 실행한다.

승인 없이 Notion 콘텐츠를 생성, 수정, 이동, archive 또는 삭제하지 않는다.

한 번의 승인은 해당 변경에만 적용한다. 이전 승인을 이후의 다른 Notion 변경에 대한 승인으로 간주하지 않는다.

---

## Secrets 및 Repository 안전

API Key, Access Token, OAuth Credential 등 비밀 정보를 코드, Notebook, Git에 포함되는 설정 파일, 로그, 예제 또는 문서에 기록하지 않는다.

로컬 secret은 `.env`를 사용하고 Version Control에 포함하지 않는다.

`.env.example`에는 공유 가능한 환경변수 이름과 placeholder만 기록한다.

OAuth Credential이나 로컬 인증 상태를 Repository에 commit하지 않는다.

사용자가 명시적으로 요청하거나 승인하지 않은 destructive 또는 irreversible 작업을 수행하지 않는다.

Git에 포함할 파일과 로컬에서만 사용할 파일의 경계를 유지한다.

---

## Dependencies 및 환경

현재 구현에 실제로 필요한 dependency만 추가한다.

개발자의 기존 전역 환경에 우연히 설치되어 있는 package에 의존하지 않는다.

Repository에 정의된 dependency와 환경 설정만으로 참가자가 실습 환경을 재현할 수 있어야 한다.

새 dependency를 추가할 때는 현재 실습에 필요한 이유가 명확해야 한다.

---

## Verification

변경의 영향도에 비례하여 필요한 수준의 검증을 수행한다.

작고 되돌리기 쉬운 변경에 대해 구현 자체를 그대로 반복하는 불필요한 테스트를 만들지 않는다.

Python 코드 변경 시에는 영향을 받는 동작을 확인할 수 있는 가장 작은 의미 있는 검증을 수행한다.

Notebook 변경 시에는 새 커널에서 해당 파일만 열어 **처음부터 끝까지 순서대로 실행할 수 있는지** 확인한다. 실행에 다른 실습 Notebook이나 `tools/` 밖의 `.py` 파일이 필요한지도 확인한다.

하나의 실습이 완료된 것으로 판단하기 전에는 **새로운 Python 3.12 가상환경**에서 Repository에 명시된 dependency와 환경 설정만으로 실행 가능한지 확인한다.

실패, 추가 변경 또는 해결되지 않은 위험이 있을 때 검증 범위를 확대한다.

필요한 검증이 통과하지 않은 상태에서 작업이 완료되었다고 판단하지 않는다.

---

## 작업 방식

중요한 변경을 시작하기 전에 관련 기존 파일과 필요한 Notion Context를 먼저 확인한다.

정당한 이유 없이 정상적으로 동작하는 기존 구현을 새 구현으로 교체하지 않는다.

변경 범위는 현재 요청에 집중한다.

기존 요구사항으로부터 안전하게 판단할 수 있는 일반적인 구현 세부사항 때문에 불필요하게 작업을 중단하거나 확인을 요청하지 않는다.

다음과 같이 프로젝트의 방향에 실질적인 영향을 주는 결정은 임의로 확정하지 않고 사용자에게 확인한다.

- 교육적 설계 또는 실습 목표의 변경
- 현재 실습 범위의 확대
- 공개 Repository 구조에 의미 있는 영향을 주는 변경
- 보안 및 권한 정책 변경
- 참가자가 경험하는 실습 방식의 중요한 변경

중요한 작업을 완료한 뒤에는 다음을 간결하게 보고한다.

- 무엇을 변경했는지
- 무엇을 검증했는지
- 아직 해결되지 않은 문제나 사용자의 결정이 필요한 사항이 있는지
